<%= form_with(model: tbldog, local: true) do |form| %>
  <% if tbldog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tbldog.errors.count, "error") %> prohibited this tbldog from being saved:</h2>

      <ul>
      <% tbldog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dogName %>
    <%= form.text_field :dogName, id: :dogName %>
  </div>

  <div class="field">
    <%= form.label :dogSize %>
    <%= form.text_field :dogSize, id: :dogSize %>
  </div>

  <div class="field">
    <%= form.label :tbl_users_id %>
    <%= form.hidden_field :tbl_users_id, id: :tbl_users_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
