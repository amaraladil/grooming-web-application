<%= form_with(model: tblinventory, local: true) do |form| %>
  <% if tblinventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tblinventory.errors.count, "error") %> prohibited this tblinventory from being saved:</h2>

      <ul>
      <% tblinventory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tblinventory_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :tblinventory_price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :tblinventory_quantity %>
  </div>

  <div class="actions">
    <%= form.submit :Submit %>
  </div>
<% end %>
